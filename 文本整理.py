import textwrap
doc='''The wrap() method is just like fill() except that it returns
a list of strings instead of one big string with newlines to separate
the wrapped lines.4488122444 '''

print(textwrap.fill(doc, width=100))

print(r'o\no')
